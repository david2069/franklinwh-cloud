# Defect & Feature Queue

Per AP-12 Change Management Policy — all items queued here before execution.

## Open

### S2 — High

| ID | Area | Description | Reported |
|----|------|-------------|----------|
| DEF-SC-ATUOEN-MAY-NOT-BE-THE-SOC-ENABLE | Mixins / CLI | **The app shows the SOC Auto Cut-off as active at 53% while `Sw1AtuoEn` reads 0.** Live 2026-09-18: `Sw1SocLowSet = 53`, matching the app's *SOC Auto Cut-off  53%* exactly, but `Sw1AtuoEn = 0`. `set_smart_circuit_soc_cutoff()` treats `AtuoEn` as the enable flag (`1 if enable else 0`) and the CLI renders it that way, so we currently report this cut-off as **disabled**. Two readings fit and the data cannot separate them: **(a)** `AtuoEn` is not the enable flag and the cut-off is live at 53%; **(b)** `AtuoEn 0` is correct and the app is displaying a stored-but-inactive threshold. **Weak circumstantial support for (a):** the sibling *Time Schedule* row on the same screen renders a state word (*On*), so a disabled cut-off might be expected to read *Off* rather than a percentage — that is a UI inference, not evidence, and is **not** being treated as resolving it. **Second observation, 14:33 the same day, strengthens the puzzle:** the threshold moved to **66** (`Sw1SocLowSet 66`, app *SOC Auto Cut-off 66%*) while **`Sw1AtuoEn` stayed 0**. So `AtuoEn` does not mean "a threshold has been set" and does not gate the app's display — it did not move when the value it supposedly enables did. It also is **not** the live battery SoC being echoed back: the battery read **99.9%** at that moment, not 66. **Not asserted either way**; nothing changed in code. Note the other circuits are uninformative — c2 `SocLowSet 0`, c3 `SocLowSet 20` with `AtuoEn 0` on both, and c3 is a factory placeholder on this 2-circuit AU gateway. **To resolve:** toggle the cut-off off and on in the app and re-read `AtuoEn` — the same decisive-observation method that settled `DEF-SC-MODE-ENUM-CONTRADICTS-SETTER`. | 2026-09-18 |
| time | app *Time Schedule* | `Sw1TimeEn` | `Sw1Mode` |
|---|---|---|---|
| 12:00, circuit on | On | `[1, 1, 1, 1]` | 1 |
| 14:33, circuit off | On | `[0, 0, 0, 0]` | 0 |

`Sw1Time` and `Sw1TimeSet` are **unchanged** across both (`12:00/12:01/13:03/14:03`, `[1,0,1,0]`) — the schedule is still configured, only the enable bits moved. A full dump of cmdType 311 confirms there is **no other schedule-enable field**: the circuit's only schedule keys are `Sw1Time`, `Sw1TimeEn`, `Sw1TimeSet` and `Sw1Freq`. **Consequence:** `_sc_schedules()` derives `any_enabled` from `TimeEn`, so `discover` and the CLI now report **no enabled schedule** on a circuit whose app screen says *Time Schedule: On*. **Two readings, undecided (AP-14):** **(a)** turning the circuit off manually **disarmed the slots** — which would make `TimeEn` the armed state rather than the configured state, and ties directly to `DEF-SC-MANUAL-OVERRIDE-SEMANTICS`, where the app's three-way turn-off dialog offers exactly the choice between deferring to the schedule and suspending it; **(b)** the app's *On* is a fixed row label meaning "a schedule exists", not a state. **(b) cannot be ruled out** because *On* is the only value that row has ever been observed showing — across three screenshots, including one taken while the slots were disabled. **Absence of an observed *Off* is not evidence it renders one.** **To resolve:** delete the schedule entirely and see whether the row changes; and re-arm the schedule without touching the switch to see whether `TimeEn` returns to 1 on its own. Until then neither our rendering nor the app's should be called wrong. | 2026-09-18 |
| DEF-SC-MODE-VS-RUNTIME-STATE-DIVERGE | Models | **`SwNMode` and runtimeData `switch_N_state` agree in some reads and not others.** 12:00: `Sw1Mode 1`, `switch_1_state 1`. 14:33: `0` and `0`. 14:41: **`Sw1Mode 1` but `switch_1_state 0`**, with the app showing the circuit **on**. So `Mode` followed the app and `switch_1_state` did not. **This does not undo `DEF-SC-MODE-ENUM-CONTRADICTS-SETTER`** — that rests on `Mode` moving 0→1 with the app's own turn-on, which still holds, and `Mode` has matched the app in all three reads. **Candidate explanations, undecided:** the two come from **different commands** (311 config vs 203 runtimeData) with different refresh behaviour, so 203 may simply lag; or `Mode` is the **commanded** state and `switch_N_state` the **achieved** one, which would make a divergence meaningful rather than stale. Load cannot distinguish them — nothing was drawing power in any of the three reads, and `switch_1_state` read **1** in the 12:00 read under exactly that condition, so an idle circuit does not by itself force a 0. **Consequence:** any consumer treating the two as interchangeable can report a circuit as off while the app says on. **To resolve:** read both repeatedly across a single toggle and see whether `switch_N_state` catches up on a later poll (lag) or stays put (distinct meaning). | 2026-09-18 |
| DEF-CARSW-CONSSUP-IS-A-METERED-SUPPLY-TASK | Discovery / Docs | **`CarSwConsSupEnable` is read as "a V2L port exists"; it is the enable flag of a *Metered electricity* task.** `discover.py:551` and `:719` both set `v2l_port=bool(sc_info.get("CarSwConsSupEnable"))`, so a **scheduled energy-supply task** is being reported as **physical port presence** — a task you can switch off, standing in for hardware that cannot appear and disappear.

**The triplet was decoded by a controlled write, 2026-09-18.** The app's *Metered electricity* screen was set to **9 kWh** with a start time of **4 Oct 2026 2:46 pm**, and the gateway moved exactly and only these:

| app field | API field | before | after |
|---|---|---|---|
| Metered electricity `9` kWh | `CarSwConsSupEnerge` | `4000` | **`9000`** |
| Start time `4 Oct 2026 2:46 pm` | `CarSwConsSupStartTime` | `'2026-09-21 14:27'` | **`'2026-10-04 14:46'`** |
| — | `CarSwConsSupEnable` | `1` | `1` (unchanged) |

**CONFIRMED:** `CarSwConsSupEnerge` is in **Wh** — 9 kWh wrote 9000, so the earlier 4000 was **4 kWh**, not the 40.00 kWh a `/100` reading would have given. `CarSwConsSupStartTime` is a gateway-local `'YYYY-MM-DD HH:MM'` string matching the app to the minute. The screen's own caption reads *"Range 0.1-9999 per aPower, accurate to 0.1"*, consistent with a 0.1 kWh (100 Wh) step.

**INFERRED, not confirmed:** that the triplet configures a metered supply on the **CarSW / V2L channel** — the name parses as Car-Switch-Consumption-Supply and the fields sit in the CarSW block, but nothing observed ties the delivery to that port. **`Enable` stayed 1 across the write, so these reads do not show what it gates** — it was already 1 beforehand. **Consequence for `v2l_port`:** whatever `Enable` means, it is one field of a three-field scheduled task, and reading it as port presence is unsupported. **Fix needs a decision, not just a rename:** there may be no port-presence signal in 311 at all, in which case `v2l_port` should report unknown rather than be derived from something else. **To resolve:** toggle *Metered electricity* off in the app and re-read `CarSwConsSupEnable`; if it goes to 0 while the hardware is untouched, the current mapping is refuted outright. | 2026-09-18 |
| DEF-SC-PROLOAD-WRITTEN-INVERTED | Mixins / `mixins/devices.py` | **Every setter writes `SwNProLoad` as the inverse of `SwNMode`; live hardware holds them equal.** Two reads of circuit 1 on 2026-09-18, straddling an app-side turn-on: **off** -> `Sw1Mode 0`, `Sw1ProLoad 0`; **on** -> `Sw1Mode 1`, `Sw1ProLoad 1`. They move **together**. Three sites write the opposite: `set_smart_switch_state()` and `set_smart_circuit_state()` both send `ProLoad = mode_val ^ 1` (`devices.py:360`, `:400`), and `devices.py:1065` sends `Sw3ProLoad: 0 if enable else 1`. So turning a circuit on writes `Mode 1, ProLoad 0` — a combination the gateway has not been observed holding in either state. A same-moment read of all three circuits agrees: **c1 `Mode 1, ProLoad 1`; c2 `0, 0`; c3 `0, 0`**. **Scope of the claim (AP-14):** n=4 across three circuits on one gateway, in both states. That is enough to **refute the inversion**, and NOT enough to say what `ProLoad` is for — the name suggests a protected-load flag, which would be configuration rather than state, and no source confirms it. `models.py:393` reads it as `pro_load_type`, also unsourced. **Unfixed, needs sign-off** (CLAUDE.md rule 6 — changes what is written to hardware). Compounded by `DEF-WRITES-NOT-VERIFIED`: these setters never read back, so if the gateway is rejecting or normalising the odd combination, nothing would report it. `tests/test_schedule_coverage.py::test_proload_is_written_as_the_inverse_of_mode_which_hardware_contradicts` pins the current behaviour so the fix is deliberate. **To resolve:** write `Mode 1, ProLoad 0` deliberately and read back — if it returns `ProLoad 1`, the gateway is correcting us and the field is derived. | 2026-09-18 |
| DEF-SC-MANUAL-OVERRIDE-SEMANTICS | Mixins / CLI | **Turning a circuit on has three meanings; our API exposes one.** When a schedule has switched a circuit off, the app asks: *"Yes, and turn-off according to the schedule"*, *"Yes, until manually turned off"*, or *"No, keep off and continue with the plan"*. So a manual ON either **defers to the schedule** or **suspends it** — a materially different outcome for an automation. `set_smart_circuit_state(circuit, True)` and `set_smart_switch_state(circuit, "ON")` are binary and **cannot express which**, so what they actually do to a scheduled circuit is unknown. `SwNMsgType` is a candidate carrier — `_update_smart_circuit_config()` already sets it to 1 for the target circuit and 0 for the others, with no recorded reason. **First data point, 2026-09-18:** the user chose **"Yes, until manually turned off"** on circuit 1 and the app's *Time Schedule* row went to **Off**, with `Sw1TimeEn` reading `[0,0,0,0]` and `Sw1Mode 1`. So that choice leaves the circuit **on with the schedule disabled** — the slots are disarmed, while `Sw1Time`, `Sw1TimeSet` and `Sw1Freq` keep the configuration. **Caveat on attribution:** `TimeEn` was **already** `[0,0,0,0]` in the 14:33 read, before this choice was made, so the clearing cannot be pinned to this action from these reads alone — only the end state is established. The other two choices are still uncaptured. **To resolve:** capture the 311 write behind each of the three choices. | 2026-09-18 |
| DEF-WRITES-NOT-VERIFIED | Mixins | **The gateway accepts writes it does not store, and most setters report the ack as success.** A rejected or invalid value returns success and is silently discarded (user report 2026-09-18; same shape as gotcha G7, where a wrong WiFi password also returns `result: 0`). Audited: only `switch_to_wifi()` read back — **every other setter treated the ack as confirmation**: `set_smart_circuit_soc_cutoff`, `set_smart_circuit_load_limit`, `set_smart_switch_state`, `set_smart_circuit_state`, `set_generator_mode`, `set_power_control_settings`. **Fixed for `set_generator_charge_schedule()`** — it now reads back, diffs field by field, and returns `{ack, verified, mismatches, note}` with `verified=None` when the check could not run, since "could not verify" is not "verified". **The rest are unfixed.** Each needs its own read-back: different endpoints, different round-trip delays, and the 311 setters would need a full re-read. The rule is now in AP-14. | 2026-09-18 |
| DEF-WIFI-SWITCH-BREAKS-SPAN | Mixins / `mixins/network.py` | **`switch_to_wifi()` can silently break a SPAN integration — now vendor-documented.** SPAN's own app note (*Integration with FranklinWH*, tech portal) shows the link is **SunSpec Modbus over TCP 502 with the aGate as CLIENT**, polling the SPAN panel's IP; the panel's **Aux Comms/ETH-1** cables to the aGate's Ethernet port; and **SPAN provides the aGate's internet over that link**. A 337 write can move the active transport (4G→WiFi observed; confirmed by U2), so that one cable may be carrying both the SPAN session and the gateway's internet. The preflight checks neither — it only asks whether *some* transport survives. Write succeeds, verify reports `connected`, panel goes quiet, **no error anywhere**. **INFERRED:** whether SPAN stays reachable over WiFi depends on topology — the documented example `192.168.0.103` is an ordinary household subnet, so it might, or might not if the link is point-to-point. Documented in `docs/SPAN_INTEGRATION.md` and in the docstring; **not enforced**. Suggested: **warn, do not block** — recoverable by re-cabling, unlike stranding the gateway. Needs sign-off (adds a call to a write path). | 2026-09-18 |
| DEF-AU-V2L-HARDCODED-FALSE | Discovery / `discovery.py` | **An assumption overrides an observation.** `discovery.py:367-368` does `if country_id == 3: has_v2l = False`, **discarding the gateway's own `v2lModeEnable`** — for AU the device's answer is never read. That is the AP-14 failure mode exactly: a market inference beating a device reading. **Even if AU V2L is genuinely unsupported the override is wrong**, because `v2lModeEnable` would then report false anyway and the hardcode buys nothing; if it is supported, the library actively hides it. **Corpus evidence that the CarSW/V2L channel is live on this AU gateway:** `CarSWCurr` is 1 in **427 of 1,267** samples; `CarSWPower` is negative (−1…−5) in ~630; `CarSWExpEnergy` and `CarSWImpEnergy` both **accumulate** (9→76 and 151→517); and `CarSwConsSupEnable` reads 1 in 9 samples. Meanwhile `v2lModeEnable` is null/0 and `v2lRunState` null throughout. So the channel reports telemetry on AU hardware while the feature flag stays off. **That is not proof AU V2L is usable** — the counters may belong to another use of the channel — but it is enough that the blanket override is unjustified. **Fix:** read `v2lModeEnable` for every market; if a regional caveat is wanted, expose it alongside the device value rather than replacing it. User is raising a FranklinWH support ticket, as the official site documents neither position. | 2026-09-15 |
| DEF-GEN-MODE-WRITES-MANUSW | Mixins / `mixins/devices.py` | **RESOLVED BY THE CORPUS — `set_generator_mode()` does not set the mode.** It posts `{"manuSw": mode}`, but correlating all 35 `updateIotGenerator` writes against the surrounding `selectIotGenerator` reads shows these are **different controls**: **(a)** four `{"mode": N}` writes with 3-7 s gaps to the confirming read each moved the **`mode`** field and nothing else — `mode` is the mode setter, CONFIRMED. **(b)** a `{"manuSw": 2}` write with a 17 s gap left `manuSw` **unchanged at 0** and instead moved **`genStat` 1→2** (Running → Cooldown, i.e. a stop). So `manuSw` is a **manual start/stop command**, not a persisted mode — it acts on generator state. The method therefore issues a manual engine command while being named and documented as a mode setter ("1 = Auto-schedule, 2 = Manual"). **Still ASSUMED:** what `mode` 1 vs 2 mean (the docstring mapping is unsourced), and the `manuSw` value→effect mapping (**n=1**). **Fix needs sign-off** — correcting the payload changes what an existing public method does (CLAUDE.md rule 6). Suggested shape: `set_generator_mode()` sends `mode`; a new, honestly named `set_generator_manual_switch()` sends `manuSw`. | 2026-09-14 |
| DEF-GEN-SOC-FIELD-NAMES | CLI / `cli_commands/gen.py` | **Introduced by me in step C.** `gen` labelled the SoC thresholds `genStartSoc`/`genStopSoc`; the fields `selectIotGenerator` actually returns are **`genStartElec`/`genCloseElec`** (observed 20 and 80). The labelled rows therefore never appeared — the values fell through to the unlabelled catch-all, so nothing was lost, but nothing was named either. Corrected, along with `genRatedPower`, `genModel`, `startDelTime` and `generatorAlarmFlag`. | 2026-09-14 |
| FEAT-SC-V2-CLOUD-CAPABILITY | Mixins / Protocol | **Planned capability: Smart Circuits V2 / aHub over cmdType 387/389.** Blocked on evidence only — design is complete in `docs/CAPABILITY_RESOLUTION_SPEC.md`, and the required capture is specified in **`docs/CAPTURE_REQUESTS.md` §Tier 2A**. **Needs a US user with Smart Circuits V2 or an aHub, on current firmware**, to open the app, view Smart Circuits and **edit a schedule**. The edit is the critical part: every captured sample is the unconfigured default (`00:00`/`23:59`, all flags `0`), so nothing reveals which array position means what. One such capture closes four tickets — this one, `DEF-SC-TIMESET-UNDECIPHERED`, `FEAT-SC-SCHEDULE-SETTER` and `DEF-CATALOG-AHUB-CIRCUIT-COUNT`. Until then 311 remains the only implemented path and the default. | 2026-09-14 |
| DEF-CATALOG-SCV2-AU-COMPAT | Const / `device_catalog.json` | **Entry 204 (Smart Circuits V2) is internally contradictory:** `country_id: 2` (US) yet `compatible_agates` includes **102 = `AGT-R1V1-AU`**, with `circuit_count: 3`. Three lines of evidence say AU is **two** circuits: the AU system datasheet ("Smart circuits module are optional with two circuits supported per aGate"); the entry's own `country_id`; and the existence of accessory **302 `Smart Circuits V1-AU`**, 2 circuits, `compatible_agates [102]`. **Currently harmless:** `discover.py` filters accessories on `country_id == site.country_id`, so the 102 entry is unreachable for an AU site, and nothing in the library reads `compatible_agates` at all. A `_note` now records the contradiction in the data itself. **Left in place rather than deleted** because SKUs have been revised over time and a future AU SC V2 cannot be ruled out. **To resolve:** confirm with FranklinWH whether SC V2 is orderable in AU/NZ. | 2026-09-14 |
| DEF-CATALOG-SKU-NOT-A-STABLE-KEY | Const / `device_catalog.json` | **SKUs are revised over time, so the catalog's `sysHdVersion → sku` map records one point in time and silently ages.** Concrete instance: catalog `sysHdVersion 7` gives model `aPower X-02-AU` the SKU `APR-05K13V2-AU`, while the vendor datasheet for that same model says `APR-05K15V1-AU` — with the aGate SKU in the same sentence (`AGT-R1V1-AU`) matching ours, so it is not describing different hardware. The user confirms SKUs have changed. **Consequence:** any identification or support workflow keyed on SKU can disagree with the label on the unit. `sysHdVersion` is what the API actually returns and is the stable key; SKU should be treated as a *display* attribute that may be stale, and ideally allow more than one value per hardware version. **Not corrected unilaterally** — the existing value may itself have come from an API response, and replacing it with a datasheet string could break identification for units already in the field. | 2026-09-14 |
| FEAT-SC-CMDTYPE-387-389 | Mixins / Protocol | **Support both Smart Circuit protocol generations.** We use cmdType **311**, whose payload is hardcoded to `Sw1*`/`Sw2*`/`Sw3*`. The fork `jkt628/franklinwh-python` uses **387/389**, whose response is an **indexed array** `data["smartSwitch"][i]["schedule"]`. Neither 387, 389 nor `smartSwitch` appears in the 44-capture corpus (highest observed: 354). **Structural argument, strong:** the aHub controls **4×240 V or 8×120 V circuits** with programmable scheduling (CONFIRMED — aHub Installation and Operations Manual p.10, p.28). **311 physically cannot represent 8 circuits** — it has three hardcoded slots — whereas an indexed `smartSwitch[i]` array scales naturally. So a second command almost certainly exists for aHub-class hardware; whether it is 387/389 specifically remains ASSUMED. **Correction to the earlier US-only reading:** Smart Circuits **V2** lists `compatible_agates [102, 103, 104]`, which **includes the AU gateway** — SC V2 is not US-only. The aHub is 120/240 Vac 60 Hz, i.e. North America, so the regional split applies to **aHub**, not to SC V2. Protocol selection must still **not** be keyed off `sysHdVersion` — see `docs/CAPABILITY_RESOLUTION_SPEC.md`. 311 stays the default (283 captured pairs). **To resolve:** a capture from an aHub or SC V2 install touching Smart Circuits. | 2026-09-14 |
| DEF-EPOCH-RENDERED-IN-CLIENT-TZ | Mixins / CLI | `discover()` and `support` convert `activeTime` / `installTime` / `createTime` with `datetime.fromtimestamp(ts / 1000.0)` and **no `tz` argument**, so Python uses the **client's** time zone, not the gateway's. Managing a Californian gateway from Sydney, a timestamp near midnight renders as the **wrong date**, silently. Sites: `mixins/discover.py:55` (`_ts_to_str`), `cli_commands/support.py:323-325`. Documented in `docs/TIME_AND_TIMEZONES.md` as a known trap. **Fix:** convert with `tz=timezone.utc` and then into the gateway's `zoneInfo`, which is available from the same `getHomeGatewayList` response the timestamps come from. Needs care — `_ts_to_str` is used for several fields and changing it shifts rendered dates for existing users, so it is a visible behaviour change rather than a silent correction. | 2026-09-13 |
| DEF-TOU-SAVE-NO-LONGER-SWITCHES-MODE | Mixins / CLI / Docs | **Backend behaviour change reported from the field (user, ~2026-06).** `saveTouDispatch` used to save a schedule **and** switch the system to TOU mode; the library asserted this twice as plain fact ("always forces a TOU mode switch"). It no longer does: the official app now reports the schedule saved and *offers* the switch, calling the ordinary mode endpoint (`updateTouModeV2` = `set_mode`) only if the user accepts. **Corpus:** CONFIRMED for the old behaviour — 38 distinct saves spanning app 2.3.1 to 2.11.0, **none** followed by `updateTouModeV2`, so the backend did it. The corpus ends **2026-03-20**, so it cannot date the change; it establishes only that the old behaviour held to that point. **Consequence:** a caller who saves a schedule expecting it to take effect now gets a stored, idle schedule and **no error**. Docs corrected with tiers, and the CLI no longer reports the new normal as an anomaly. **Remaining:** whether `set_tou_schedule()` should grow an opt-in `activate=True` that performs the switch is an API-affecting change and needs sign-off (CLAUDE.md rule 6) — the two-step pattern works today. | 2026-09-13 |
| DEF-SDCP-MEANING-UNSOURCED | CLI / Const | **Reported by the user: their non-US gateway carries `sdcpFlag` set, and they are not in California or any SDCP utility area.** Two problems. (1) `support.py` expanded SDCP as "Smart Device Control Program" with **no citation**; the user reads it as **San Diego Community Power**, a Californian CCA. Circumstantial support for a utility reading: the sibling field is `sdcpCompanyFlag`, and `sdcpFlag` also appears in `getTouCompanyListPageV2`, a utility-company list (19 samples). Neither expansion is sourced, so the label now asserts neither. (2) **A US-utility programme flag set on an Australian gateway is direct evidence that these flags are not enrolment** — it is not possible to be enrolled with a San Diego provider from Australia. That bears on `DEF-JA12-ENTRANCE-VS-JOINED` and on `sgipEntrance`/`bbEntrance`, which have no join field at all. **Corpus note:** `sdcpFlag` is `false` in all 4,000 `getEntranceInfo` samples, so the user's gateway reporting it set is a **change since the captures** — worth a fresh capture to date it. **To resolve:** ask FranklinWH, or observe whether the flag gates a UI affordance (`sdcpSwitchModeTip` exists in `endpoints.py`) rather than a tariff. | 2026-09-12 |
| DEF-NEM-TYPE-ZERO-UNRESOLVED | Const / Mixins | **Open question, raised by the FWHAI agent: is `nemType: 0` a genuine "NEM 2.0" or an unset sentinel?** Corpus evidence: `result.nemType` is observed as **{0: 661, 3: 11}** across 672 `getTouDispatchDetail` responses. Every 0 sits on a gateway whose country is Australia with the retailer Amber — and Net Energy Metering is a US (chiefly Californian) scheme that does not exist there. The only other value, **3, occurs solely in captures carrying US markers and is absent from the catalog entirely** (`nem_types` covers 0/1/2 only), which is independent evidence the catalog's domain was guessed rather than sourced. **Mitigated, not resolved:** no NEM label is now asserted outside the US, and `flags.nem_type_raw` preserves the wire value. **To resolve:** a capture from a US install with a known NEM enrolment, to see whether it reports 0, and what 3 means. Until then `nem_types[0]` should be treated as unverified. Note `result.template.nemType` is **absent in all 672 samples** — code reading that path resolves None and fails safe by accident, not by design. | 2026-09-12 |
| DEF-ETH-PORT-IDENTITY-UNCONFIRMED | Const / Mixins | **Harder than first filed — vendor port naming is revision-dependent and reverses meaning.** System Installation Guide p.62: household cable to **Eth1**. Commissioning Guide p.7: household cable to **Eth2** on aGate X 1.1, where **Eth1 is the Debug port**; aGate X 1.3/1.3.1 has a single **ETH** port. So "Eth1" is the internet port in one document and the debug port in another, and on 1.3 units the API exposes `eth0` *and* `eth1` while only one physical port exists. Confirms the user's report that one port is FranklinWH-internal — it is the Debug port. Labels are keyed to the API field and assert nothing; the preflight keeps distrusting non-active Ethernet, which is now the only defensible default rather than mere caution. **To resolve, per hardware revision:** plug the household cable in and observe which API field acquires the address. Record the `sysHdVersion` alongside the answer. | 2026-08-31 |
| DEF-WIFI-BAND-UNFILTERABLE | Mixins / CLI | The aGate joins **2.4 GHz only** (Installation Guide p.59) but cmdType 335 discovers both bands and returns no band field, so a 5 GHz-only SSID is ranked at full signal, accepted by a 337 write, and never associates — then is indistinguishable from a wrong password, since neither surfaces an error. Mitigated, not fixed: every scan carries the caveat and the verify timeout names it as a cause. **A real fix needs a band signal the API does not currently expose** — check whether a newer cmdType or app version returns band/channel, or correlate against a router-side AP list. This is the most likely cause of a P2-7-style write succeeding and the link never coming up. | 2026-08-31 |
| DEF-OPERATOR-RSSI-SCALE-UNSOURCED | Const / Mixins / CLI / Docs | The claim that `operatorRSSI` / `4GSignalStrength` use a **0-52 vendor scale** is asserted in 10 places, all as unsourced comments, and is **not supported by the corpus**. Evidence found while auditing after `DEF-CONNTYPE-ENCODING-WRONG`: `operatorRSSI` appears **4 times total, every one of them the value 22** — four samples of a single value cannot establish a range. `4GSignalStrength` does not appear in the corpus at all (the design doc quotes one instance of 45). Meanwhile `runtimeData.signal`, the mobile signal, has **20,542 samples spanning 0-99 and clustering at 20-22** — matching `operatorRSSI`'s only observed value, and it is an established 0-100 percentage (D3, same scan). That correlation suggests the two are the same quantity and that `operatorRSSI` is a **percentage**, making `22/52` wrong. **Deliberately not changed:** n=4 is too thin to justify flipping it, and asserting a percentage on this evidence would repeat the `DEF-CONNTYPE-ENCODING-WRONG` mistake in the opposite direction. **To resolve:** `tools/network_probe.py observe` over a period where cellular signal actually varies, or find where 52 originally came from. If it is a percentage, everything currently rendering `n/52` is understating a healthy signal. | 2026-08-30 |

### S3 — Medium

| ID | Area | Description | Reported |
|----|------|-------------|----------|
| FEAT-SC-EVENT-HISTORY | Mixins | **The app has a Smart Circuits Event History we cannot read.** Its Events tab lists entries like *"Smart Circuits disconnected — The Smart Circuits have been disconnected as the battery SOC is below the set threshold"*, dated 2024-09-14 and 2025-03-24. `docs/CLI_SCHEMA_COMMAND.md` and the `network` notes both state the API exposes no event log — `selectDeviceRunLogList` is a static alarm-code dictionary. That is evidently **not the whole picture**: the app renders real, timestamped events, so a source exists and we have not found it. **To resolve:** capture the app opening Smart Circuits → Events. Would also settle whether the same source carries the network transitions the probe currently has to poll for. | 2026-09-18 |
| DEF-SC-DATE-SETTING-UNMAPPED | Models / Schema | **The app's "Date Setting" mode is not modelled.** Timing Supply offers **Once only / Everyday / Cycle interval**, with an *Execution time* date shown for *Once only*. No field in `SmartCircuitDetail` carries the mode, so a schedule read back cannot be told apart as one-shot or recurring — and the retained `date` alone does not distinguish them. `SwNFreq` ({0: 85, 15: 69, 60: 4}) is the leading candidate and may encode *Cycle interval*, but that is **ASSUMED**; `SwNAtuoEn` is already taken by the SOC cut-off. **To resolve:** set each of the three modes in the app and capture the resulting 311 write. Blocks a faithful schedule setter as much as the slot layout did. | 2026-09-18 |
| FEAT-SC-OVERLOAD-CUTOFF | Mixins / Docs | **Overload cut-off is a documented feature with no representation here.** Vendor: *"If the load exceeds the main panel's capacity, the system will automatically disconnect circuits to prevent overload damage."* Distinct from SOC Auto Cut-off, which is off-grid only. No field in the cmdType 311 payload has been identified as its configuration or status, so it is unclear whether it is configurable at all or purely automatic. `set_smart_circuit_load_limit()` writes a per-circuit amp limit and may be related, but that link is **unestablished**. **To resolve:** look for it in the app's Smart Circuits screens and capture any write. | 2026-09-18 |
| DEF-SPAN-MODBUS-WRITE-GATING | Mixins / Docs | **Open question: does `spanFlag` gate the aGate's Modbus WRITE access?** User hypothesis (2026-09-18): enabling SPAN is the only documented route by which the aGate is told to speak Modbus, so the flag may act as an internal control — plausibly permitting writes only once configured. Worth settling because it would explain a Modbus capability with no user-facing switch of its own. **The strong form is already refuted:** on the reference gateway `diag` reports `SPAN Panel ○ Not configured` and `Local Modbus (502) ● Open` **in the same run**, so the flag does not gate the listener. **The narrow form is untested** — our probe is a bare TCP connect, which proves something accepts a connection and nothing about reads, writes or register permissions. **To resolve:** attempt a Modbus register write with `spanFlag = 0`, then repeat with SPAN configured; `franklinwh-local-bridge` already has a Modbus path. Pick a register harmless to change — it is a write to live hardware. Recorded in `docs/SPAN_INTEGRATION.md`. | 2026-09-18 |
| DEF-MODBUS-502-PROBE-AMBIGUOUS | Mixins / CLI | **Our 502 probe and the SPAN integration are different services on the same port number.** `discover(probe_local=True)` and `get_connectivity_overview(deep_scan=True)` probe **502 on the aGate**. The SPAN link runs **502 on the SPAN panel**, with the aGate as Modbus *client* (SPAN app note). A result from one says nothing about the other, and `diag` renders `Local Modbus (502): ● Open` with no indication which is meant. Currently harmless — it is labelled informational — but a reader on a SPAN site would reasonably misread it as the SPAN link being healthy. **Fix:** name it explicitly ("aGate Modbus server"), and if SPAN reachability is ever wanted, probe the configured SPAN IP instead, which the gateway already stores. | 2026-09-18 |
| DEF-V2L-MERGE-TOPOLOGY | Mixins / Docs | **`SwMerge` and V2L are related, and `set_v2l_mode()` may model only part of it.** Per user report (2026-09-15, not vendor-confirmed): a US unit has three circuits; **1 and 2 are 110/120 V and merge into a single 240 V circuit** via `SwMerge`; and with a **Generator Module fitted, that merged pair can act as the V2L INPUT in place of the normal generator port**. So V2L is partly an **input-side topology choice**, not just an output toggle — whereas `set_v2l_mode()` is already marked SPECULATIVE and assumes V2L maps to a single `Sw3Mode` write. Those may describe different arrangements. Catalog corroboration: SC V1 has `v2l_requires_generator: true`, SC V2 `false`, and **SC V1-AU has `v2l_port: false`** — so AU is unaffected, consistent with the reference gateway never exercising this. `discover` already models the merge correctly (SC1+SC2 → logical SC1 at 240 V, SC3 → logical SC2). **Operating conditions added by the user 2026-09-18 (same source, still not vendor-confirmed):** the merged 240 V pair is V2L-capable **only when the system is off-grid, a generator is configured, and the generator is turned off**. That makes V2L-over-the-merged-pair a **mutually exclusive alternative to running the generator** — the same input path cannot serve both — and it means eligibility is a **runtime state**, not a static hardware fact: it depends on `gridFlag`/off-grid state and on `genStat`, both of which change minute to minute. **Consequence:** `snap.flags.v2l_eligible`, which is computed from accessory catalog entries alone, answers "could this hardware ever do V2L" and not "can it right now". A caller reading it as the latter will be wrong whenever the system is grid-connected or the generator is running. Neither reading is wrong to expose — they need different names. **To resolve:** a capture from a US install toggling V2L, with and without the merge, would show whether `Sw3Mode` is the whole story. Until then `set_v2l_mode()` stays speculative and should not be presented as verified. | 2026-09-15 |
| DEF-SC-METER-SCALE-DISAGREEMENT | Mixins / cross-project | **This library and `franklinwh-local-bridge` disagree on Smart Circuit meter scaling, and the bridge's own comment says its result is implausible.** The bridge applies **÷10 to volts** and **÷100 to energy** (`circuits.py` `meter()`), reasoning from `freq: 499 → 49.9 Hz`. Its comment then records that `Sw1Volt: 1020 → 102.0 V` **"does not match AU 230 V mains"**, attributing this to both circuits being off at capture. This library deliberately applies **no** divisor to volts (only `freq` has an established one). **A divisor that yields an implausible value is evidence against the divisor**, not just against the sample. Possibilities: the factor is not 10 for volts; the reading is a per-leg artifact (see `docs/AC_TOPOLOGY.md`); or an off circuit reports something other than mains. **To resolve:** read the meter with a circuit switched **on** and compare against a known load. Worth resolving jointly — two projects should not ship different volt readings for the same field. The bridge's `energy_lifetime_kwh` note (a lifetime counter, not a daily figure) is a useful finding this library should adopt regardless. | 2026-09-15 |
| DEF-AC-TOPOLOGY-NO-US-SAMPLE | Const / `ac_topology.py` | `SPLIT_PHASE_COUNTRY_IDS = {2}` (US) rests on the market standard and the US datasheets, **not on an observation**. The corpus is a single Australian gateway and no US metrics have been obtained — the user reports being unable to get anyone in the US to share theirs — so nobody has confirmed what a genuine split-phase site reports in `gridVol1`/`gridVol2`. The AU branch is well-evidenced; the US branch is inference. **Consequence is mild** — being wrong means showing a caveat that is not needed, not hiding data. **To resolve:** any US gateway's `--json schema --live` or `--json support` output would settle it. | 2026-09-14 |
| DEF-PHASE-FLAG-AMBIGUOUS | Models / Discovery | `isThreePhaseInstall` is a boolean and `models.py` documents `0` as **"split-phase"** — a US-centric reading. On an AU/NZ site `0` means **single-phase**, a different topology with no L2. So the library can tell three-phase from everything else but **cannot distinguish split-phase from single-phase** from the flag alone. `discover` bridges the gap by inferring from `country_id == 3`, which is **ASSUMED**: it matches the AU/NZ datasheet but is derived from market, not read from the device, and would not cover a US 208 V commercial install or a new market. **To resolve:** find a field that reports topology directly, or model it as a three-valued capability (`single` / `split` / `three`) resolved once, with the region inference as an explicit documented fallback. | 2026-09-14 |
| DEF-CATALOG-AHUB-CIRCUIT-COUNT | Const / `device_catalog.json` | The aHub entry (253) described it only as an aPower companion — no circuit capability at all — and FWHAI likewise models it as a PV / aPower S companion (`has_ahub`, `remote_solar`). **The vendor manual says otherwise:** aHub provides Smart Circuits Management for up to **4×240 V or 8×120 V circuits** with programmable scheduling (p.10, p.28). Entry corrected with `provides_smart_circuits`, both circuit maxima, and the citation. **Downstream consequence not yet addressed:** `ResolvedCapabilities.circuit_count` is derived as `2 if generation == 1 else 3` and has no aHub branch, so an aHub install is under-reported by up to 5 circuits. Fixing that needs to know how the API enumerates aHub circuits — which is the same unknown as `FEAT-SC-CMDTYPE-387-389`. | 2026-09-14 |
| DEF-CATALOG-COMPATIBLE-AGATES-MIXED-TYPE | Const / `device_catalog.json` | `compatible_agates` is a **list of ints** for most accessories but the **string `"ALL"`** for aPBox (251) and Split-CT (252). Nothing in the library reads the field today, so it is latent — but any code doing `gw_id in acc["compatible_agates"]` would silently mis-handle `"ALL"` (substring matching against a string), and iteration would yield characters. **Fix:** normalise to a list, or define a sentinel the reader must handle explicitly. Worth doing before the capability work in `CAPABILITY_RESOLUTION_SPEC.md` starts consuming it. | 2026-09-14 |
| FEAT-SC-SCHEDULE-SETTER | Mixins / CLI | **No write path for Smart Circuit schedules.** `set_smart_switch_state(circuit, "SCHEDULE")` puts a circuit into schedule mode, but nothing can define **what** the schedule is — there is no setter for `SwNTime` / `SwNTimeEn` / `SwNTimeSet`, so a circuit can be told to follow a schedule it has no way of receiving. `_update_smart_circuit_config()` already performs the read-modify-write 311 cycle these would use, so the mechanism exists. **Blocked on semantics, not effort:** the array pairing is unverified (see `DEF-SC-SCHEDULE-NOT-RENDERED`) and `SwNTimeSet` is entirely undeciphered — observed as `[1,0,1,0]` on two circuits and `[0,0,0,0]` on a third, with no captured change. Writing a guessed layout to a live circuit could switch loads at the wrong times. **To resolve:** set a schedule in the official app and capture the resulting 311 write; that single capture gives both the pairing and `SwNTimeSet`. API-affecting — needs sign-off (CLAUDE.md rule 6). | 2026-09-14 |
| DEF-JA12-ENTRANCE-VS-JOINED | Const / Mixins / CLI | **Does `ja12Entrance` mean eligible-and-offered, or actually joined?** Unresolved, and the corpus cannot settle it. The two flags come from different endpoints and never co-occur in a response: `ja12Entrance` from `getEntranceInfo` (109 samples), `isJoinJA12` from the gateway list (9). Correlated by gateway: **no captured gateway has `ja12Entrance=1` together with a known `isJoinJA12`** — the decisive case has never been observed. The two gateways where both are known are `0`/`False`, which agree under either hypothesis and so carry no information. Indirect evidence they differ: **only JA12 has an `isJoin*` sibling** — no `isJoinSGIP` or `isJoinBB` exists anywhere in the corpus, and a second field would be redundant if `*Entrance` already meant joined. (`joinFlag` is unrelated — it belongs to `getNpsShowTip`, a survey prompt.) **To resolve:** one capture from a gateway reporting `ja12Entrance=1`, with its gateway-list `isJoinJA12`. `flags.ja12_joined` now records the second half, so the next such gateway settles it by observation. The same doubt applies to `sgipEntrance` and `bbEntrance`, which have no join field at all. | 2026-09-12 |
| DEF-BMS-211-STATE-CODES-UNDECODED | CLI / `cli_commands/bms.py` | `franklinwh-cli bms` prints `bmsState`, `mosState`, `inverterStatus`, `DCDCStatus`, `runMode`, `switchState` and `alarmLevel` as raw integers, where the FWHAI UI shows words (`Discharging`, `All Active`, `Running`, `Closed`). **The obvious fix is wrong:** `const/states.py`'s `BMS_STATE` and `PCS_STATE` decode `runtimeData.bms_work` and `runtimeData.pe_stat` — per-pack **list** fields from **cmdType 203** — and are already used correctly in `discover.py`. The fields above are scalars from the **cmdType 211** payload. Similar names, different endpoint, unverified domains. Corroborating evidence that they differ: the UI shows DCDC as "Running", a value `DCDC_STATE` does not contain. **To resolve:** capture one `franklinwh-cli --json bms` from live hardware (the JSON is the raw payload), establish each field's domain, then add 211-specific maps. Do not reuse the 203 ones. | 2026-09-12 |
| FEAT-MODE-DYNAMIC-LIST | Mixins | `get_gateway_tou_list` returns gateway-specific modes (e.g. `peak` instead of `Time of Use`). Client should retrieve and use this dynamic list instead of hardcoded `OPERATING_MODES`. **⏸ ON HOLD — client impact assessment required.** | 2026-03-26 |

> **Design Notes (FEAT-MODE-DYNAMIC-LIST — ON HOLD)**
>
> The mobile app pattern: on startup it fetches the gateway's active mode list dynamically via `get_gateway_tou_list`, then only presents modes that are supported by that specific hardware configuration (e.g. AU grid may not expose `peak`, commercial gateways may expose additional modes).
>
> **Breaking change risk:** Downstream clients (HA integrator, automations) that call `set_mode("peak")` or compare against hardcoded `OPERATING_MODES` keys will break silently if the gateway returns a different mode name than expected.
>
> **Open design questions before execution:**
> 1. **Graceful degradation**: If a client requests a mode not in the gateway's dynamic list, should we: (a) raise `InvalidModeException`, (b) silently no-op with a warning, or (c) attempt the set and let the API reject it?
> 2. **Mode name normalisation**: The gateway returns display strings (e.g. `"Time of Use"`, `"Self-Consumption"`). Do we expose these raw, or map them to the existing canonical slug keys (`tou`, `self_consumption`)?
> 3. **Caching**: The mode list rarely changes (hardware config). Should it be fetched once on `Client.__init__`, or lazily on first `get_mode()`/`set_mode()` call?
> 4. **Emulator support**: The emulator needs a `get_gateway_tou_list` synthetic endpoint before live testing can safely proceed.
> 5. **Documentation**: [COMPLETED] Downstream UI constraint handling and fallback states are now fully specified in `docs/OPERATING_MODES_GUIDE.md`.


### S4 — Low

| ID | Area | Description | Reported |
|----|------|-------------|----------|
| FEAT-AUTH-ABSTRACT | Client / API Core | Auth strategy pattern (PasswordAuth → OAuthAuth → ApiKeyAuth) — ready for OAuth-day when FranklinWH introduces token-based auth | 2026-03-21 |
| FEAT-DOCS-OPENAPI | Docs | Generate OpenAPI/Swagger spec from HAR capture of full app lifecycle | 2026-03-21 |

---

## Resolved

| ID | Area | Description | Fixed In | Commit |
|----|------|-------------|----------|--------|
| FEAT-SC-SCHEDULE-WRITE | Mixins / CLI | **BUILT — `set_smart_circuit_schedule()` and `fwh sc --set-schedule`.** Blocked for weeks on not knowing the slot layout; every part of that was settled on live hardware 2026-09-18 and the writer mirrors what was observed rather than inventing a shape. Four slots are **two start/end pairs** (`TimeSet [1,0,1,0]`), `TimeEn` arms slots individually (the app's *Time Schedule* row read **Off** with the bits zeroed and **On** with them set), `Freq` is a cycle in **days** with **0 = "Once only"**, and execution date is **`base + k x cycle`** (2026-06-19 + 2x60 = 2026-10-17, matching the app exactly).

**Deliberate limits.** It **does not touch `Mode`/`ProLoad`** — arming a schedule is not switching a circuit, and the setters that write those two disagree with hardware (`DEF-SC-PROLOAD-WRITTEN-INVERTED`), so the write path stays clear of them. It **never invents a date**: with `cycle_days=0` the base date *is* the execution date, so a slot that has never been written requires an explicit `base_date` rather than being filled with the client's idea of today, which can be the wrong day on a gateway in another zone. An empty window list **disarms without discarding** the configured times, mirroring what the gateway did across an observed turn-off. Overlaps and midnight-spanning windows are rejected, as in `set_generator_charge_schedule()`.

It **reads back and diffs**, returning `{ack, verified, mismatches, note}` with `verified=None` when the check could not run — making it the third setter in the library to verify rather than trust an ack (`DEF-WRITES-NOT-VERIFIED`). 40 tests. **Not yet exercised against hardware** — field testing pending. | 2026-09-18 |
| DEF-SC-TIMEEN-CLEARED-BY-MANUAL-OFF | Models / Mixins / CLI | **RESOLVED — `SwNTimeEn` is the schedule enable and our rendering was right; the app display had lagged.** Filed when the app reported *Time Schedule: On* at 14:33 while `Sw1TimeEn` read `[0,0,0,0]`, leaving two readings open: **(a)** the manual off disarmed the slots, or **(b)** the app's *On* is a fixed row label. **(b) is refuted by observation** — at 14:41 the same row rendered **Off**, so it is a real state indicator. With `Sw1TimeEn` still `[0,0,0,0]` and the row now reading *Off*, the field and the app agree, and the 14:33 *On* was a **stale app display** that had not yet caught up with a change already on the gateway.

| time | app *Time Schedule* | `Sw1TimeEn` | `Sw1Mode` | app switch |
|---|---|---|---|---|
| 12:00 | On | `[1, 1, 1, 1]` | 1 | on |
| 14:33 | On (stale) | `[0, 0, 0, 0]` | 0 | off |
| 14:41 | **Off** | `[0, 0, 0, 0]` | 1 | on |

**Method note (AP-14):** the filed entry said *"absence of an observed Off is not evidence it renders one"* and named the test that would settle it. One more observation produced the *Off*. Nothing in the library changed — `_sc_schedules()` derives `any_enabled` from `TimeEn`, which is correct. **The lesson is about comparing against a screenshot:** the app is a second cache, not ground truth, and a disagreement between it and a gateway read can be the app being behind rather than a field being misread. | 2026-09-18 |
| DEF-SC-MODE-ENUM-CONTRADICTS-SETTER | Const / Mixins | **RESOLVED on live hardware 2026-09-18 — `SwNMode` is ON/OFF, and the constant was wrong.** The decisive observation filed with the defect was carried out: the circuit was turned on in the app and re-read. **11:57 off** -> `Sw1Mode 0`, runtimeData `switch_1_state 0`; **12:00 on** -> `Sw1Mode 1`, `switch_1_state 1`. `SwNMode` tracks the runtime switch state, so **0=Off, 1=On** — which is what `set_smart_switch_state()` had always written. `const.states.SMART_CIRCUIT_MODE` ({0: Manual, 1: Schedule, 2: Smart/Auto}) was unsourced and rendered an **ON** circuit as "Schedule" in `discover`. Corrected, with the live derivation in the comment. **2 remains ASSUMED, and is now doubtful.** It is what `set_smart_switch_state(circuit, "SCHEDULE")` sends, and it has never been seen on the wire. A later read on the same day is evidence against it existing at all: with a schedule **configured** on circuit 1 and the app's *Time Schedule* row reading *On*, `Sw1Mode` read **0**, not 2. The gateway appears to express "scheduled" as `Mode` (the switch) **plus** `SwNTimeEn` (the slots), not as a third `Mode` value — so writing 2 may set the switch to a value the firmware does not define. Not yet a filed defect because nothing has written 2 and read the result back; that is the test. Its label says "assumed" rather than claiming "Smart / Auto". Independent corroboration: the sibling `franklinwh-local-bridge` reads `Sw{cid}Mode == 1` as "circuit is on" against the local 1409 payload, reached separately. Same class as `DEF-CONNTYPE-ENCODING-WRONG` — a plausible enum asserted from a comment. Resolving it exposed `DEF-SC-PROLOAD-WRITTEN-INVERTED`. | 2026-09-18 |
| DEF-SC-DATE-CHANGED-BETWEEN-READS | Models | **NOT A DEFECT — the schedule had been edited in the app between the reads.** Filed when circuit 1's slot dates moved between two reads minutes apart, during a window when a turn-on had failed on a session timeout, so the reads were suspect. The follow-up read explains it: the whole schedule was **replaced**, not drifted — `Sw1Freq 60 -> 0` and all four `Sw1Time` slots moved from 2026-06-19 16:02/17:03/17:04/17:05 to 2026-09-18 12:00/12:01/13:03/14:03. The app confirms the new configuration exactly: *Cycle interval* **Once only**, *Execution time* **18 Sept 2026**, *Time period* **12:00-12:01 pm** and **1:03-2:03 pm**. An edit, not instability. **Two things it confirms as a side effect:** `SwNFreq` **0 = "Once only"**, pinning the zero case of the days unit; and with zero cycles the base date **is** the execution date, the degenerate case of the `base + k x cycle` derivation confirmed at `Freq 60`. **The observations voided with it stay void** — `SwNMode` being unchanged after the failed turn-on was from that same window and was superseded by a clean read, not rehabilitated by this. | 2026-09-18 |
| DEF-SC-EXECUTION-DATE-UNLOCATED | Models | **RESOLVED — the execution date is DERIVED, not stored.** The date in `SwNTime` is the schedule's **base date**, `SwNFreq` the cycle in days, and the app's *Execution time* is the **next occurrence**: `base + k × cycle`. Confirmed arithmetically against a screenshot taken before any later change — base `2026-06-19` with a 60-day cycle yields 2026-06-19 → 2026-08-18 → **2026-10-17**, exactly the *"Execution time: 17 Oct 2026"* the app displayed. Now exposed as `base_date` and `cycle_days`. The next occurrence is **not computed** here: it needs *today* in the **gateway's** time zone, which the snapshot does not carry, and using the caller's clock would be wrong by up to a day across zones. | Unreleased | pending |
| DEF-SC-FREQ-UNMODELLED | Models / Schema | **RESOLVED — `SwNFreq` is the Cycle interval, in DAYS.** Confirmed against live hardware 2026-09-18: the user set a schedule on circuit 1 with the app showing **"Cycle interval: 60 days"**, and a read-back returned **`Sw1Freq = 60`**, while unscheduled circuit 2 returned `Sw2Freq = 0`. My earlier note guessed minutes from the corpus values {0, 15, 60} — the number was right and **the unit was wrong**, which a distribution alone could never have settled. | Unreleased | pending |
| DEF-SC-TIMESET-UNDECIPHERED | Const / Models | **RESOLVED by the app's own UI.** The Timing Supply screen refuses a third entry with **"Only two time slots can be scheduled"**, and renders each slot as a start–end range (`4:02 pm–5:03 pm`, `5:04 pm–5:05 pm`). So the four `SwNTime` entries are **two ranges**: (0,1) and (2,3). That matches `SwNTimeSet` `[1,0,1,0]` marking start/end/start/end, and independently corroborates `franklinwh-local-bridge`'s reading of the field as a per-slot action. `SwNTimeEn` is the per-slot enable. Recorded as `role`, `range` and `pairing: "two ranges of start/end"`. | Unreleased | pending |
| DEF-SC-DATE-DISCARDED | Mixins / CLI | **Introduced by me, 2026-09-18.** I documented the date half of `SwNTime` as "a placeholder ('2000-01-01') in every observed sample" and stripped it. Both halves were wrong: the corpus carries **real dates** (2025-10-04 ×484, -05 ×300, -17 ×226, -18 ×138) alongside 2000-01-01 ×2876, and the app shows an **"Execution time"** date for a *Once only* schedule. So `2000-01-01` is the **unset sentinel**, not a placeholder, and stripping it discarded the most important field of a one-shot schedule. Now retained as `date`, with the sentinel normalised to `None`. The claim came from a sample survey I never ran — the tell was asserting "every observed sample" without a count. | Unreleased | pending |
| DEF-SPAN-FLAG-IS-CONFIG-NOT-DETECTION | Mixins / CLI | `get_span_setting()` documented `spanFlag` as **"1 = SPAN panel detected"**, asserting an autodetection mechanism the evidence does not show. SPAN's app note documents it as a **deliberate installer action** — Settings → Modbus → SPAN Panel, entering the panel's IP and port 502 on an *installer* account. So **`0` means "not configured", not "no panel present"**: a panel can be cabled and wired while the flag reads 0. `diag` compounded it by rendering "● Active"/"○ Inactive", which implied the panel was *answering* — the flag says nothing about reachability. Now "● Configured"/"○ Not configured". `get_connectivity_overview` gains `span_configured` while retaining `span_connected` unchanged, since downstream consumers read it. Same class as the `*Entrance` flags reading "Enrolled". | Unreleased | pending |
| FEAT-GEN-CHARGE-SCHEDULE | Mixins / CLI | **Generator charge schedule is now writable.** `set_generator_charge_schedule(windows, confirm=True)` and `fwh gen --schedule START-END` (repeatable, up to three; `off` disables all). Payload **mirrors the captured app write** rather than an invented shape — disabled windows send `chargeNEn: 0` with no times, exactly as the app does, since whether omitting times clears them is unestablished. **Times are gateway-local wall clock** (user-confirmed) and are **not** converted; the CLI warns about this before writing, as it is the likeliest way to get it wrong from another time zone. **Validation the backend may not perform:** overlapping enabled windows are rejected — the official app prevents entering them, which suggests the backend may accept them silently — as are midnight-spanning windows, never observed and therefore unverified. Approved by the user 2026-09-15 (CLAUDE.md rule 6). | Unreleased | pending |
| FEAT-SC-GEN-UNIFIED-SURFACE | Mixins / CLI | **Steps A-C delivered** (`docs/SMART_CIRCUITS_GENERATOR_DESIGN.md`). Added `get_smart_circuit_detail()` and `get_generator_detail()`, composing config (cmdType 311) with live metrics (353/354) — previously two methods behind two CLI commands. New `fwh sc --detail [--circuit N]` and **`fwh gen`**, the generator's first command of its own. **Read-only throughout.** A circuit absent from the metrics payload gets `metrics: None`, never zeros, so "not reported" stays distinguishable from "reading zero". Metrics are raw except `frequency_hz`, the one established divisor; generator attribution is marked **INFERRED** in both the payload and the rendered output, since those fields are unprefixed and identified by position. Schedule slots carry `pairing: unverified`. **Steps D (generator writes, needs sign-off) and E (schedule writes, blocked on `DEF-SC-TIMESET-UNDECIPHERED`) remain open.** | Unreleased | pending |
| DEF-ACCESSORY-POWER-OPTION-TYPE | Mixins / `mixins/devices.py` | `get_accessories_power_info(option=1)` declared an **int** default while every branch compared against a **str**, so `1 == "1"` was False, the default call matched nothing, and it silently returned the raw payload instead of the documented Smart Circuits view. Unnoticed because the only in-repo caller passes `"0"`, which worked by luck. Both types are now accepted. The method also **hardcoded circuits 1 and 2**; circuits are now discovered from the keys the gateway actually returned, so a three-circuit system reports three and an absent circuit is **omitted rather than zero-filled** — a circuit that does not exist must not appear as an idle one. Handles the vendor's inconsistent casing (`SW1Curr` but `Sw1Volt`). Values stay **raw**: only `freq` has an established divisor. Step A of `docs/SMART_CIRCUITS_GENERATOR_DESIGN.md`. | Unreleased | pending |
| DEF-AC-TOPOLOGY-INCONSISTENT | CLI / `ac_topology.py` | The API reports L1/L2 for every install, including single-phase AU/NZ sites (`230/240 VAC L/N/PE`) where no second conductor exists. `discover` handled this; `bms`, `support` and `schema` presented the pair as independent readings. Resolved **once** in a new `ac_topology.legs_are_real()` rather than re-derived per renderer — per-renderer logic is what produced the divergence. `bms` takes `countryId` from the `getDeviceInfoV2` call it already makes, so this costs no extra request; `support` gains `ac_legs_are_real` **additively**, leaving the existing `grid_voltage_l1_v`/`_l2_v` keys untouched for downstream consumers; `schema` states the caveat in rendered output, not only in a comment. Undetermined topology shows the values **with** a caveat rather than hiding them — discarding data on a guess is worse than qualifying it. | Unreleased | pending |
| DEF-SC-SCHEDULE-NOT-RENDERED | CLI / `cli_commands/sc.py` | `franklinwh-cli sc` showed **no schedule at all** on current firmware. The renderer handled only the legacy V1 `SwNOpenTime`/`SwNCloseTime` integers, and the cmdType 311 payload in the corpus contains **none of them** — it carries the V2 arrays `SwNTime` / `SwNTimeEn` / `SwNTimeSet` instead. Those were already parsed into `SmartCircuitDetail` and already emitted by `sc --json`; only the terminal view dropped them. Now rendered per slot with its enable state. **AP-14:** listed positionally, not as start→end windows — whether the four entries are two windows or four independent slots is **not established**, since every captured sample is the unconfigured default (00:00/23:59, all flags 0). | Unreleased | pending |
| FEAT-JA12-CLI-SURFACE | CLI / `cli_commands/schema.py` | `query_compliance_capacity()` (`ja12/queryComplianceCapacity`, California Title 24) existed in `TouMixin` but was reachable from no CLI command. Now surfaced by `schema --live`, **gated on the gateway reporting `ja12Entrance`** so a CA-specific request is never sent from other markets. **AP-14:** the endpoint has **zero captured responses** in the corpus, so the payload is rendered generically and passed through verbatim in `--json` — no schema is declared and no units are claimed for fields nobody has observed. The output says so. Once a real response is captured a `JA12_SCHEMA` can replace this. | Unreleased | pending |
| DEF-ISJOINJA12-NEVER-READ | Const / Mixins | `isJoinJA12` was declared in `device_catalog.json` but read by no code. Now read into `flags.ja12_joined` from the gateway-list object `discover()` already walks, guarded on key presence so an absent field stays **None** (unknown) rather than becoming False — only 9 corpus samples carry it at all. Kept separate from `flags.ja12` rather than merged, because the two come from different endpoints and their relationship is unresolved. **The semantics question is NOT closed** — see `DEF-JA12-ENTRANCE-VS-JOINED`. | Unreleased | pending |
| DEF-PROGRAMME-ENTRANCE-OVERCLAIM | CLI / `cli_commands/discover.py` | `discover` rendered SGIP, Battery Bonus and JA12 as **"Enrolled"** whenever the corresponding `*Entrance` field was 1 — asserting enrolment from a field whose name denotes an entry point being offered. Same class of unverified claim as `nemType: 0` rendering "NEM 2.0", and reachable: `sgipEntrance=1` occurs 75 times in the corpus. The asymmetry decides the wording — 0 safely implies not enrolled, 1 does not safely imply enrolled — so the 1 case now reads "Available or enrolled". JA12 additionally prefers `ja12_joined` when the gateway reports it, which answers the question directly. | Unreleased | pending |
| DEF-BB-GROUP-MISLABEL | CLI / `cli_commands/schema.py` | `bbDischargePower` was grouped as **"Backup Battery"**. `bb` is **Battery Bonus**, the Hawaiian Electric programme — `discovery.py` has always documented the sibling flag correctly (`bb: bool  # Hawaii Battery Bonus`); only the schema group misread the abbreviation, so an enrolled Hawaii user saw a programme discharge cap presented as a generic backup setting. Corpus corroboration: `bbDischargePower` is null in all 3,930 samples alongside `bbEntrance=0` in all 4,001 — the cap populates only once enrolled. Relabelled "Battery Bonus (HI)", matching how `discover` already renders "JA12 (CA)". | Unreleased | pending |
| DEF-SCHEMA-TOTALS-POWER-FILTER | CLI / `cli_commands/schema.py` | `schema --filter power` rendered the `stats.totals` header with zero rows: substring matching against group names, and no TOTALS_SCHEMA group contains "power". CURRENT_SCHEMA happens to have "Power Flow" and "Power Measurements (211)", so the filter looked like it worked while half its output silently vanished. Fixed via `_totals_filtered_out()`, applied to **both** the JSON and terminal paths — the original report identified only one. `power` and `energy` now match every totals group, since all of them are cumulative energy; a hardcoded subset would have drifted as groups are added. Unrelated filters still exclude, so `--filter` is not a no-op. | Unreleased | pending |
| DEF-TOU-NEXT-WAVE-OVERFLOW | CLI / `cli_commands/tou.py` | `tou --next` used a 12-wide WAVE column while `WAVE_TYPES` contains "Super Off-Peak" at 14 characters, so it ran into DURATION with no separator (`Super Off-Peak10h 00m`). Header, separator rule and row all widened to 15, matching the full schedule table. Guarded by a test that checks **every** WAVE format spec in the module against the longest label, rather than the one line fixed. | Unreleased | pending |
| CI-TAG-BRANCH-GUARD | CI / `.github/workflows/publish.yml` | **Was already implemented and left open in this ledger — a traceability miss, not a code one.** `verify-branch` asserts the tag commit is reachable from `origin/main` via `git merge-base --is-ancestor`, and gates the build job. Verified present on this branch. | Unreleased | `fdc6a0b` |
| CI-VERSION-CONSISTENCY | CI / `.github/workflows/publish.yml` | **Also already implemented and left open.** `verify-versions` asserts the tag name, `pyproject.toml` version and `__version__` agree, and gates the build job alongside `verify-branch`. Verified present on this branch. | Unreleased | `fdc6a0b` |
| DEF-DISCOVER-TOU-WRONG-ENDPOINT | Mixins / `mixins/discover.py` | **Reported by the FWHAI agent as "pto_date is always empty" — confirmed, and it was one symptom of a larger fault.** `discover()` read five fields from `get_gateway_tou_list()`, but the two TOU endpoints carry **disjoint** field sets. Measured over the corpus: `getGatewayTouListV2` (n=1271) has `vppSocVo` 98% and `todayVppVo` 100% but `ptoDate`/`nemType`/`template` **0%**; `getTouDispatchDetail` (n=672) has `ptoDate` 72%, `nemType` 100%, `template` 100% but `vpp*` **0%**. So `pto_date`, `electric_company`, `tariff_name` and `der_schedule` could never populate. **Worse, `nem_type` reported a confident wrong value:** absent `nemType` defaulted to 0, which the catalog maps to "NEM 2.0", so every discover claimed NEM 2.0 regardless of tariff. **The proposed one-line fix would have inverted the bug** — swapping to the detail endpoint fixes those five and breaks the two VPP fields that did work. Now reads each field from the endpoint that carries it, guarded separately. | Unreleased | pending |
| DEF-CONNTYPE-ENCODING-WRONG | Models / Const / CLI / Docs | **Supersedes DEF-STATUS-CONNTYPE-ENUM, which was not a defect — my change for it was a regression, now reverted.** `models.py` annotated `runtimeData.connType` as `0=4G, 1=WiFi, 2=Ethernet` and `const/devices.py` warned the encoding was incompatible with `currentNetType`. Neither claim was sourced. The corpus contradicts both: across **20,471 runtimeData samples** `connType` is observed only as `{2: 559, 3: 19797, 4: 115}` — 0 and 1 never occur, and 3 dominating matches a gateway that lives on WiFi. `status.py` was therefore **already correct**; changing it to a new `CONN_TYPE_NAMES` map turned every healthy gateway into `Unknown (3)` on the most-used command. Reverted; the map deleted; the annotation, the const warning and design gotcha G2 corrected with the evidence. Found because the user asked for proof the network settings were right — the tests I had written pinned my belief, not the wire. | Unreleased | pending |
| DEF-NULL-VALUE-GET-SWEEP | Package-wide | **283 sites** rewritten from `x.get(k, {})` to `(x.get(k) or {})`. `.get(key, default)` returns the default only when the key is **absent**; the API returns many keys present-with-null, where it hands back `None` and the next access raises. Applied by a bracket-aware codemod, each file re-parsed before being written. The transform is correct by construction — the default already declared the field as a dict or list — and its only behaviour change is replacing a legitimately-empty dict/list with an equal one. **A first pass excluded variable-keyed sites as 'local catalogs only'; that was wrong**, and a behavioural test caught it: `support.py` had `net.get(iface, {})` looping over an API response. All keys are now swept and guarded. Two guard tests plus behavioural coverage of the largest concentration. | Unreleased | pending |
| FEAT-CLOUD-UPTIME-HEARTBEAT | Client / `heartbeat.py` / CLI | Warranty terms require the aGate to hold cloud connectivity, so sustained loss needs flagging — and `awsStatus` cannot do it, having been observed reading zero while the gateway answered through the cloud. Added `GatewayHeartbeat`, recorded at `_mqtt_send`, the single **gateway** round-trip chokepoint (a REST success would only prove *we* reached the cloud). Persists `last_success` **and** `last_attempt`: one alone cannot distinguish "gateway is down" from "nothing has run since Tuesday". Exposed via `client.get_gateway_heartbeat()` and rendered by `fwh network status`, which escalates to an error on a sustained outage. Disk writes are rate limited but forced on outage onset and recovery, so neither transition is lost. Thresholds are deliberately left to Home Assistant. | Unreleased | pending |
| DEF-SUPPORT-RSSI-DBM | CLI / `cli_commands/support.py` | Four sites labelled a value `dBm` that was not. Three printed `operatorRSSI`, a **0-52 vendor scale** (gotcha G3), now rendered `n/52 (vendor scale)`. The fourth read the module's own `mobile_signal_dbm` — a **deprecated alias whose value is a 0-100 percentage** — and relabelled it dBm, so `4G 45 dBm` was a percentage wearing the wrong unit twice over. It now reads `mobile_signal_pct` and prints `%`; the `_dbm` output key is retained for compatibility but is no longer consumed internally. Guarded by a test that walks string literals so no `dBm` label can return. Completes the G3 sweep begun in Phase 1. | Unreleased | pending |
| DEF-SUPPORT-NETTYPE-ENUM | CLI / `cli_commands/support.py` | Filed as `DEF-SUPPORT-CONNTYPE-ENUM` on the assumption it was a third encoding. **Mis-diagnosed:** all three call sites read `currentNetType`; only the variable and key names say "connType", and that misnomer is what disguised it. It is therefore the same defect as `DEF-DIAG-NETTYPE-ENUM` — a local bitmask table indexed positionally, so `currentNetType=1` rendered "WiFi". Now shares `const.devices.NETWORK_TYPES`, with no local copy left to drift. The `conn_type in (4,5,6,13)` cellular check became `== 4`; 5/6/13 were bitmask values that cannot occur. A test fixture asserting behaviour for `currentNetType=5` was encoding the defect and was corrected to 4 against the documented 1-4 domain (HAR corpus shows 2, 3, 4). | Unreleased | pending |
| DEF-PII-MAC-BLINDSPOT | CI / `scripts/check_pii.py` | The PII gate matched emails, Sydney lat/lon, gateway serials and version strings but **not MAC addresses**, so it passed on a fixture containing a real cellular-modem MAC copied out of live terminal output into this **public** repo. It was caught by reading, which is not a control. Added a MAC pattern: obvious placeholders (`AA:BB:CC:`, `DE:AD:BE:`, `00:00:00:`, `11:22:33:`) pass, everything else fails. Five real MACs already committed before the check existed are allowlisted by value with a comment forbidding extension — scrubbing those means rewriting `main`'s history, a separate decision. Verified both ways: the leaked address now fails the scan, placeholders pass, and the current tree is clean across 251 files. | Unreleased | pending |
| FEAT-LOCAL-REACHABILITY-PROBE | Mixins / CLI | Added `get_network_state(probe_local=True)` and `fwh network status --probe-local`. Probes **TCP 9000** — the aGate local broker protocol, the mobile app's "Direct Connection" — falling back to **22**. Port verified against the sibling `franklinwh-local` project (`franklinwh_local/transport.py` `DEFAULT_PORT`), not assumed; an earlier guess of 10000 was wrong. 502 is deliberately excluded from liveness because Modbus listens only when enabled. This is a **discriminator, not a verdict**: combined with cloud round-trip success it separates "gateway alive, WAN/cloud broken" from "gateway down" from "caller is not on its LAN". Opt-in (it is I/O), never gates a write, and records "no address to probe" as `reachable=None` rather than as a failure. | Unreleased | pending |
| DEF-AWS-STATUS-SOURCE | Mixins / CLI | 317 and 339 each carry an `awsStatus` and were observed disagreeing on the same gateway at the same moment (317=1, 339=0, with 317 matching reality). Resolved by not trusting **either** self-report as the verdict: `cloud.gateway_reachable` is now the primary signal — tautological, since the payload reached us through the cloud, which is exactly why it is evidence. Both raw flags are exposed as `aws_status_317_raw` / `aws_status_339_raw` for comparison, the `schema` health check reports the disagreement when they differ, and `net status` labels them "Gateway self-report" rather than presenting them as the answer. `aws_connected` and `internet` keep their existing meaning and value so downstream consumers do not break. Sustained-outage alerting is deliberately left to the consumer — see the follow-up. | Unreleased | pending |
| DEF-PREFLIGHT-UNVERIFIED-ETHERNET | Mixins / `mixins/network.py` | **S2, introduced by P2-2.** The write-safety preflight counted any Ethernet port holding an address as a surviving fallback. At least one aGate Ethernet port is reserved for FranklinWH-internal use on a segment with no internet route, and the API exposes nothing that distinguishes it from a user port — so a write could have been allowed on a fallback unable to catch it. An Ethernet port now counts only when it is the **active** transport (proven: the gateway is answering through it). Opt back in with `trust_ethernet=True` / `--trust-ethernet`. WiFi and 4G are unaffected, and the primary use case (idle 4G as the lifeline) is unchanged. Latent rather than live, since both ports were address-less. | Unreleased | pending |
| DEF-ETH-LINK-SHARED-FLAG | Mixins / CLI | 339-extended exposes one `EthConnectRouterStatus` for both ports, so eth0 and eth1 necessarily read the same value while being presented as independent per-port observations. Resolved by honesty rather than inference — no Ethernet hardware is available to validate a per-port derivation, and inventing precision the wire does not provide is what caused the neighbouring defects. Entries for eth0/eth1 now carry `link_shared: True`; `net status` and the `schema` health check say so. `available` is unaffected, since it also requires a per-port address. | Unreleased | pending |
| DEF-DIAG-PRIMARY-LINK-WORDING | CLI / `cli_commands/diag.py` | `diag` labelled the active transport `Primary Link`, implying a setting the user chose. The aGate selects for itself — 17 of 19 observed transport changes followed no command (gotcha G9). Relabelled `Active Link`. The `primary` JSON key is unchanged and covered by a test, so downstream consumers are unaffected. | Unreleased | pending |
| DEF-SCHEMA-DHCP-NOT-RENDERED | CLI / `cli_commands/schema.py` | The capability inventory advertised `interfaces[].dhcp` but the state table beneath it had no such column — reachable only via `--json`. Added an `addr src` column rendering `dhcp` / `static` / `—`. Named `addr src` rather than anything reservation-flavoured: the field says the aGate is a DHCP *client*, and whether the router holds a reservation is not exposed by any FranklinWH endpoint. | Unreleased | pending |
| DEF-NET-STATUS-CLOUD-CAVEAT | CLI / `cli_commands/network.py` | `fwh network status` printed `AWS ✗ Internet ✗` with no indication those cmdType 339 flags are known-unreliable, while `schema` warns about the identical data. A reader would reasonably conclude the gateway was offline while it was answering through the cloud (§2.5a). Warning now printed at the point of display, and only when a flag is actually down. Introduced by P2-6. | Unreleased | pending |
| DEF-DIAG-NETTYPE-ENUM | CLI / `cli_commands/diag.py` | `diag` rendered `Active Network: WiFi+Ethernet` on a gateway on WiFi only. A local bitmask-style table was indexed with `currentNetType`, which is positional (`1=Eth0, 2=Eth1, 3=WiFi, 4=4G`). Verified the bitmask encoding matches no field this code reads, then replaced it with the shared `const.devices.NETWORK_TYPES`. Unknown ids now render `Unknown (n)` instead of being coerced to a wrong label. Gotcha G2. 9 new tests, 8 of which fail against the unfixed code, including a guard that inspects string literals to stop the bitmask table being reintroduced. | Unreleased | pending |
| DEF-DIAG-SIGNAL-SOURCE | CLI / Mixins | `diag` reported `WiFi Signal: 0.0%` and `4G/Mobile Signal: 0.0%` on a gateway where 339 reported 72% WiFi and 317 reported `operatorRSSI` 22 with the SIM active. Both came from 203/runtimeData, which is unpopulated on this firmware. WiFi now prefers 339-extended `WifiSignalStrength` (same 0-100 unit, safe swap) and falls back to runtimeData only when the extended payload is absent. **4G deliberately does NOT overwrite the `mobile_signal` percentage** — `operatorRSSI` is a 0-52 vendor scale, so it is exposed as `mobile_signal_raw` with `mobile_signal_scale`; writing 22 into a percentage key would repeat the unit-mislabelling defect, not fix it. Additive keys only. | Unreleased | pending |
| DEF-DIAG-RSSI-DBM | CLI / `cli_commands/diag.py` | `diag` printed `RSSI: 22 dBm` for cellular; `operatorRSSI` is a 0-52 vendor scale (gotcha G3). Now renders `22/52 (vendor scale)`. Same mislabelling Phase 1 fixed in `discovery.py`, `schema.py` and `support.py`; `diag.py:373` had been missed. | Unreleased | pending |
| DEF-DIAG-GATEWAY-NONE-GUARD | CLI / `cli_commands/diag.py` | `diag` Gateway section failed with `'NoneType' object has no attribute 'get'`. `.get(key, default)` returns the default only when the key is **absent**; the API returns `solarHaveVo`, `runtimeData` and `result` **present with a null value**, so `.get(k, {})` handed back `None`. The exception aborted the block before `operating_mode`, `run_status` and `soc` were assigned — the cause of the blank Mode/Run Status and SoC 0% in the same report. Fixed with `or {}` at each site, plus `int(x or 0)` on the two int casts and `or []` on the gateway list this block iterates. 11 new tests, 8 of which fail against the unfixed code. | Unreleased | pending |
| DEF-DIAG-BACKUP-FALSE-NEGATIVE | CLI / `cli_commands/diag.py` | `diag` reported `Backup Links: None viable` on a gateway holding a working 4G lifeline. Viability was derived from `signals.mobile_signal` (203/runtimeData), which reads 0.0 on hardware where 317 `operatorRSSI` reports 22/52 with the SIM active — understating fallback availability, the same class of error the 2026-08-08 preflight correction fixed. Now sourced from `get_network_state()['available_transports']`, which encodes the corrected per-transport rule. Legacy derivation retained as a fallback so a failed extra call cannot make the row worse. 13 new tests including a reproduction guard and a render smoke test. | Unreleased | pending |
| DEF-NOTIFICATION-SETTINGS-TYPO | Mixins / `mixins/account.py` | Fixed critical URL typo in get_notification_settings() correcting it from `/hes-gateway/terminal/selectTerPusselectEventClassification` to `/hes-gateway/terminal/selectEventClassification`. | Unreleased | pending |
| DEF-USER-RESOURCES-DRIFT | Mixins / `mixins/account.py` | Resolved endpoint path drift and response format drift in get_user_resources() for user resources lookup. | Unreleased | pending |
| FHAI-ROGUE-SC-SWITCH-STATE | Mixins / `mixins/devices.py` | **[RETROACTIVE — Unauthorized Agent Change]** An FHAI agent self-authorized adding `set_smart_switch_state(circuit, state)` to `DevicesMixin` and bumping the package from `0.4.6 → 0.4.7` (commits `98fbb2e`, `4780404`, 2026-04-01) to satisfy an FHAI runtime dependency — without an approved ticket, implementation plan, or `"explicit declaration of break change"` authorization. **Post-hoc audit (2026-04-27):** Implementation reviewed and accepted as functionally correct (proper type coercion bool/str/int, correct MQTT cmdType 310 payload, no public API removal). The AP-1 violation is documented here for traceability. v0.4.7 is retained on PyPI. | v0.4.7 | `98fbb2e` |
| OPT-MODE-COMPOSITE-HINT | Mixins / `mixins/modes.py` | `get_mode()` and `set_mode()` called `getDeviceCompositeInfo` unconditionally, duplicating `get_stats()` REST GET cost when called in the same tick. Added keyword-only `composite_hint: dict | None = None` to both methods — when supplied, the API call is skipped entirely. Fully backward-compatible (default None = original behaviour). Documented in `docs/API_COOKBOOK.md` Transport Architecture section. | Unreleased | pending |
| DEF-TOU-CURRENT-COUNT | CLI / `cli_commands/tou.py` | `tou --current` footer used `len(strategies)` (total seasons) instead of rendered seasons count. Fixed + improved grammar (`1 season` not `1 seasons`). | 2026-04-14 | `f7d7042` |
| FEAT-METRICS-PYTHON-METHOD-COUNTERS | Metrics / ClientMetrics | Added `calls_by_python_method` dict to `ClientMetrics` + `record_python_call()`. Instance-level `_apply_method_tracking()` on `Client` (runs after `_apply_method_cache` — tracker fires on cache hits). Opt-in via `track_python_methods=True`. CLI `metrics` updated. `API_METRICS_VISUALIZATION.md` corrected with agent warning. | 2026-04-14 | `638328c` |
| DEF-BMS-STATE-SWAP | Constants / CLI | Fixed inverted BMS Charging/Discharging states and enforced dict lookup. | Unreleased | `27cfaf4` |
| DEF-BLANK-STATS-PASSTHROUGH | Mixins / Models | Added `is_stale` caching to `get_stats()` to prevent API glitches sending zero-value payloads | Unreleased | `5b29114` |
| DEF-GRID-STATE-ENUM | Models / Mixins / CLI / FHAI | Replaced `grid_outage: bool` with `GridConnectionState` enum (four states: `CONNECTED`, `OUTAGE`, `SIMULATED_OFF_GRID`, `NOT_GRID_TIED`). Startup cache for `NOT_GRID_TIED` via `get_entrance_info()`. Dual-gate on `main_sw[0]==0` OR `offgridreason!=null` before calling `get_grid_status()` — handles firmware relay reporting lag (~5–10s after go-off-grid). Live-verified: `CONNECTED → SIMULATED_OFF_GRID → CONNECTED` cycle. Full docs in `API_COOKBOOK.md §Grid Connection State`. | Unreleased | `76debfb` |
| DEF-TOU-LOG-NOISE | Mixins | `get_tou_info()` emitted INFO-level logs on every poll cycle, flooding HA system logs | Unreleased | `pending` |
| DEF-RELAY-INV | CLI / Mixin | Relay states inverted in `diag` and `discover`: firmware encodes `1=OPEN`, code displayed `1` as CLOSED. Fixed `fmt_relay`, `main_sw` index comment, `bool(val)→not bool(val)` storage, `stats.grid_relay2` attr path, and `ON/OFF→OPEN/CLOSED` labels. | Unreleased | `pending` |
| DEF-GRID-PROFILE-DYNAMIC-ID | Mixins | `get_grid_profile_info(2)` hardcoded `systemId=0` returning empty payloads; CLI crashed with `UnboundLocalError` due to string `requestType` | Unreleased | `pending` |
| FEAT-TEST-INTEGRATION | Tests | Live gateway integration test suite (read-only endpoints) with JSON schema validation against `franklinwh_openapi.json` | Unreleased | `pending` |
| FEAT-TEST-API-PROXY | Tests | FastAPI-based local proxy emulator (`emulator/`) intercepting requests and returning synthetic responses for offline structural testing | Unreleased | `pending` |
| DEF-STATS-DOUBLE-SLASH | Mixins | `get_power_details` and `get_power_by_day` prepend `/hes-gateway/` to `self.url_base` causing 404 from CloudFront due to double slash `//` | 2026-03-26 | `pending` |
| DEF-CLIENT-URL-BASE | Client / API Core | 34 methods used hardcoded `DEFAULT_URL_BASE` instead of `self.url_base`; base URL not configurable | 2026-03-21 | `2b55919` |
| DEF-AUTH-LOGIN-TYPE | Client / API Core | `_login()` hardcoded `type: 1` (installer) — should be `type: 0` (user) for homeowner accounts | 2026-03-21 | `cba2b6d` |
| DEF-MODE-GETMODE | Mixins | `get_mode()` chained 3 API calls without error handling; refactored with try/except, separate variables, proper error returns | 2026-03-20 | `b86cdc6` |
| DEF-MODE-SUPPRESS | Mixins | `suppress_params` typo — `get_unread_count()` and `set_mode()` silently ignored flag | 2026-03-20 | `9dc7f52` |
| DEF-MODE-ALARMS | Mixins | `currentAlarmVOList` stringified then iterated over characters | 2026-03-20 | `9dc7f52` |
| DEF-MODE-CRASH | CLI Commands | `franklinwh-cli mode` crashes with NoneType when `get_mode()` fails | 2026-03-20 | `7dd2702` |
| DEF-MODE-NAME | CLI Commands | `mode` command displays `self_consumption` instead of `Self-Consumption` | 2026-03-20 | `76d341e` |
| DEF-SITE-DETAIL | Mixins | `get_site_detail()` sent `siteId=''` — `siteId` not in `fetcher.info`; fixed to resolve via `get_home_gateway_list()` + gateway serial match | 2026-03-25 | `09e1fdd` |
| FEAT-CLI-DISCOVER-VERBOSE | CLI Commands | Enhanced `discover` with 3 verbosity tiers, feature flags, Hybrid A+B JSON catalog | 2026-03-23 | `multiple` |
| DEF-CLIENT-TIMEOUT | Client / API Core | `httpx.TimeoutException` propagated raw; now caught in `__post`/`__get` → `ApiTimeoutError` with friendly CLI message | 2026-03-25 | `f38d456` |
| FEAT-AUTH-CLI-OPTION | CLI Commands | `franklinwh-cli --installer` flag passes `LOGIN_TYPE_INSTALLER` to `TokenFetcher` | 2026-03-25 | `f38d456` |
| DEF-CLI-TOU-PRICE | CLI Commands | `franklinwh-cli tou` and `--price` do not display actual pricing rates; should default to zero if none | 2026-03-27 | |
